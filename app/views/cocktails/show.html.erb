<h1><%= @cocktail.name %></h1>
<ul>

  <% @cocktail.doses.each do |dose| %>
  <li>
  <%= dose.description %>
  <%= dose.ingredient.name %>
  <%= link_to 'Delete', dose, method: :delete %>
  </li>
  <% end %>
</ul>
<br>
<%= link_to "Back", cocktails_path %><br>
<%= link_to "Add a new ingredient", new_cocktail_dose_path(@cocktail) %>
